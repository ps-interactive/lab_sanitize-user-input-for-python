# Description
A developer creates a simple employee portal that authenticates users against a SQLite database.   

To quickly build the SQL statement, they use an **f-string**.

🚨 Unfortunately, this allows user input to become part of the SQL command.